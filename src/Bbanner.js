import React, { useEffect } from "react";
import "./Banner.css"
import axios from "./axios"
import requests from "./requests";

function Banner () {
  const [movie, setMovie] = useState ([]);

  useEffect(()=>{
    const request = await.get(reques)
  })
}

